# macOS
.DS_Store
.Thumbs.db

# Hugo build output
public/
resources/_gen/

# IDE files
.vscode/
.idea/
*.swp
*.swo

# Environment and secrets
.env
.env.*
*.key
*.pem

# Node (if ever used)
node_modules/

# OS generated
*.log
Thumbs.db
ehthumbs.db

# Original unprocessed photos
Anand.jpg

# Internal files that were untracked in commit 4436a28 (pre-launch privacy audit,
# May 17 2026). Listed explicitly so a stray `git add .` cannot re-add them.
architecture.jsx
docs/web-filter-categorization.md
docs/MIGRATE-TO-HUGO-MODULES.md
docs/EXECUTION_PLAN.md

# Locally kept full-resolution image originals (pre-compression).
# Never commit or publish.
static/images/_originals_backup/
.image-originals/

# Private outreach / social campaign material (pitches, dataset working copies,
# LinkedIn carousel sources, playbooks). Internal-only, never publish from
# the blog repo.
docs/outreach/
outreach-assets/

# Unpublished post drafts held outside content/ so Hugo never builds them
# and git never tracks them. Move a file into content/posts/ to publish.
drafts/

# Internal Claude session files (workflow notes, not for the public repo)
CLAUDE.md
NOTES.md
