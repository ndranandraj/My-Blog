# macOS
.DS_Store
.Thumbs.db

# Hugo build output
public/
resources/_gen/

# IDE files
.vscode/
.idea/
*.swp
*.swo

# Environment and secrets
.env
.env.*
*.key
*.pem

# Node (if ever used)
node_modules/

# OS generated
*.log
Thumbs.db
ehthumbs.db

# Original unprocessed photos
Anand.jpg

# Internal files that were untracked in commit 4436a28 (pre-launch privacy audit,
# May 17 2026). Listed explicitly so a stray `git add .` cannot re-add them.
architecture.jsx
docs/web-filter-categorization.md
docs/MIGRATE-TO-HUGO-MODULES.md
docs/EXECUTION_PLAN.md
docs/security-headers.md
docs/medium-republish-checklist.md

# Locally kept full-resolution image originals (pre-compression).
# Never commit or publish.
static/images/_originals_backup/
.image-originals/

# Private outreach / social campaign material (pitches, dataset working copies,
# LinkedIn carousel sources, playbooks). Internal-only, never publish from
# the blog repo.
docs/outreach/
outreach-assets/

# Unpublished post drafts held outside content/ so Hugo never builds them
# and git never tracks them. Move a file into content/posts/ to publish.
drafts/

# Internal Claude session files (workflow notes, not for the public repo)
CLAUDE.md
NOTES.md
.redesign-backup/

# ---------------------------------------------------------------------------
# Second privacy audit, 2026-08-10. These were TRACKED and should not have been.
# Adding them here does not untrack them: run the `git rm --cached` commands in
# docs/outreach/gitignore-remediation.md once, then commit.
# ---------------------------------------------------------------------------

# Another project's internal working material. The MoSPI benchmark is unpublished
# research into a government portal, with issues filed and emails already sent to
# a ministry. Its run sheet and results template must not sit in a public repo.
docs/projects/

# Internal planning documents. Same class as the ones caught in the May 2026 audit.
docs/homepage-redesign-plan.md
docs/homepage-redesign-execution-plan.md
docs/image-optimization.md

# Binary blobs that were never meant to be versioned.
*.zip
Website blog design improvement.zip

# Retired site sections kept locally for reference.
archive/

# Hugo build lock
.hugo_build.lock
